# Public runtime configuration for the Vercel-deployed admin app.
# See apps/police-pwa/.env.example for full notes on Vercel + Next.js
# NEXT_PUBLIC_* semantics; without these set the deployed app cannot
# reach the gateway and login fails with "Failed to fetch".

NEXT_PUBLIC_API_BASE=
NEXT_PUBLIC_DEFAULT_TENANT=demo
